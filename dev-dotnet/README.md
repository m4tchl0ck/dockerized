# dev-dotnet

Development container for .NET, built on `dev-base`.

## Included tools
- .NET SDK (`dotnet10-sdk`) installed via `apk`

## Notes
- Uses the Alpine package for .NET, so the SDK version follows the repository package.
