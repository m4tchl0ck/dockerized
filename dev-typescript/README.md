# dev-typescript

TypeScript/Node development container based on the Microsoft devcontainers image.

## Included tools
- Node.js 22 from `mcr.microsoft.com/devcontainers/typescript-node:22`
- zsh set as the default shell
- GitHub CLI (`gh`)
- GnuPG utilities (`gnupg2`, `gnupg-agent`) and `socat`
