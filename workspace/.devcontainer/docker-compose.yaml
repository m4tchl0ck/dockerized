services:
  development:
    build:
      context: .
    network_mode: host
    env_file:
      - path: ../.env
        required: true
    environment:
      - AWS_PROFILE=localstack
      - AWS_ENDPOINT_URL_DYNAMODB=http://localstack:4566
    command: sleep infinity
    volumes:
      - ..:/workspaces:cached
